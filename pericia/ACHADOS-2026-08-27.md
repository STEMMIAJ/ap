# Auditoria das skills instaladas — 2026-08-27

Levantamento feito lendo os `SKILL.md` instalados. Cada afirmação abaixo tem
origem no arquivo citado; nenhum número foi estimado.

## O que mudou

As skills estão em duas gerações, e elas discordam sobre a raiz do acervo.

| Geração | Skills | Raiz que assumem |
|---|---|---|
| 14/08/2026 | `cartao-de-progresso`, `complexidade-e-honorarios-pericia`, `entregar-abrindo`, `erros-recorrentes`, `fila-unica-continuidade`, `nunca-mandar-clicar` | `~/Desktop/STEMMIA Dexter/`, `~/Desktop/Mesa/`, `~/Desktop/_MESA/`, `~/Documents/Arquitetura de Memória - GPT-Claude/` |
| 27/08/2026 | `aj-nomeacoes-browser`, `checkpoint-pericial-browser`, `handoff-sessao`, `mapear-seletores-browser`, `medir-e-liberar`, `organizar-exame`, `pre-processar-pedido`, `rotina-aj`, `triagem-pericial-browser` | `~/Desktop/STEMMIA/`, `~/Desktop/ACERVO-LAUDO-IDEAL/` |

Nenhuma skill de 27/08 cita `STEMMIA Dexter`, `_MESA` ou `Desktop/Mesa`.
A divisão é limpa: são as pastas antigas.

## Consequência direta para o fluxo de honorários

`complexidade-e-honorarios-pericia` é a única skill que ainda aponta para
`~/Desktop/STEMMIA Dexter/STEMMIAJUD/`. Todos os 9 caminhos executáveis dela
(`triar_acervo.py`, `cli_fatores.py`, `cli_prova.py`, `cli_proposta.py`,
`rqe_cnj.py`, `extrair_texto_pdf.py`, a pasta `HONORARIOS/`, `01-casos-ativos/`
e `00-LOGICA-UNICA/`) estão sob essa raiz. Com o rename, a seção 7 dela não
executa.

## Rotas quebradas

`triagem-pericial-browser` encerra a análise escolhendo uma próxima rota. Sete
dos nomes que ela cita não existem entre as skills instaladas:

`calculo-honorarios` · `gestao-prazos` · `peticao-cowork` ·
`responder-quesitos` · `Gerador de Roteiro Pericial` · `gerar-laudo` ·
`revisao-laudo`

Mais duas fora dessa tabela: `prazos-pericia` (citada por
`complexidade-e-honorarios-pericia`, seção 10) e `continuar-em-outra-sessao`
(citada por `checkpoint-pericial-browser`).

`calculo-honorarios` é provavelmente o nome anterior de
`complexidade-e-honorarios-pericia` — a rota de honorários da triagem morre em
silêncio.

O `manifest.json` reforça: 15 skills `custom` (datadas de 25/07 a 25/08)
constam no manifest sem pasta correspondente em disco.

## Fora de STEMMIA

| Pasta | Veredito | Base |
|---|---|---|
| `~/Desktop/ACERVO-LAUDO-IDEAL/` | **vivo** | citada por 3 skills de 27/08; `PLANO-TPMED-CONSULTAVEL-v2.md` é fonte de `medir-e-liberar` e `organizar-exame`; `HANDOFF-SESSAO.md` é destino de `handoff-sessao` |
| `~/Desktop/_MESA/` | suspeito | só `erros-recorrentes` (14/08) cita — `INVENTARIO-CLAUDE.md` e `40-CLAUDE/erros-claude/` |
| `~/Desktop/Mesa/Painel/` | suspeito | só `fila-unica-continuidade` e `entregar-abrindo` (14/08) — `00-MESTRE.md`, `TASKS.md` |
| `~/Documents/Arquitetura de Memória - GPT-Claude/` | suspeito | só `cartao-de-progresso` (14/08) — `reports/DIARIO-DE-PROGRESSO.md` |
| `~/Desktop/Mesa/Pastas/Perícia e fluxo/PROJETO CALCULADORA HONORÁRIOS/` | **veneno** | cópia congelada de 2026-07-16; o `detecta_custeio.py` dela classifica CUSTEADO como GRATUIDADE (falta o patch de 18/07) e recusa proposta devida |

`_MESA` e `Mesa` são duas grafias distintas, ambas só no lote antigo.

## Novidades dentro de STEMMIA que a skill de honorários não conhece

- `~/Desktop/STEMMIA/CENTRO-DE-COMANDO/anotar.py` (`handoff-sessao`)
- `~/Desktop/STEMMIA/COWORK-STEMMIA/PLANO-DESTRAVA-ESTADO.json` e `gerar_painel_destrava.py` (`pre-processar-pedido`)
- `~/Desktop/STEMMIA/STEMMIAJUD/05-automacoes/medir_caso.py` (`medir-e-liberar`)
- `~/Desktop/STEMMIA/STEMMIAJUD/bin/caso` (`checkpoint-pericial-browser`)

## Como confirmar

`auditar_caminhos_skills.py` resolve cada caminho contra o disco real e marca
`EXISTE` / `RECUPERAVEL` (conserta só trocando a raiz) / `AUSENTE`.

```bash
python3 auditar_caminhos_skills.py --csv ~/Desktop/auditoria-skills.csv --abrir
```

Sai com código 1 quando encontra raiz morta ou rota quebrada.

## Não confirmado

`<pendente>CONFIRMAR</pendente>` se `STEMMIA Dexter` foi renomeada para
`STEMMIA` ou se as duas coexistem — o auditor responde na coluna
`corrigido_para`.
